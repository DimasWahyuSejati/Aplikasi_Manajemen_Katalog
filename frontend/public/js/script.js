/**
 * Script global untuk aplikasi.
 *
 * Catatan: Fungsi-fungsi utility (confirmDelete, formatCurrency, dll.)
 * sudah dipindahkan ke helpers.js untuk menghindari duplikasi.
 * File ini sekarang hanya berisi logika global yang tidak cocok di tempat lain.
 */

// File ini sengaja minimal — semua fungsi shared ada di helpers.js